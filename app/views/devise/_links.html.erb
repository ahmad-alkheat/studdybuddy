<% unless current_user.nil? %>
	<%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
<% end %>

<%= link_to 'New request', new_post_path %>

<%= link_to 'Browse requests', authenticated_root_path %>

